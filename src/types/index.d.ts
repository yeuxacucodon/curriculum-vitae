export type education = {
  title: string;
  studies: string;
  year: string;
};

export type project = {
  title: string;
  preview: string;
  repo: string;
  used: string[];
};

export type social = {
  icon: string;
  link: string;
  title: string;
};
