export type education = {
  title: string;
  studies: string;
  year: string;
};

export type project = {
  title: string;
  preview: string;
  repo: string;
  used: string[];
};

export type social = {
  icon: string;
  link: string;
  title: string;
};

export type human = {
  avt: string;
  name: string;
  profession: string;
  phone: string;
  email: string;
};

export type language = {
  lang: string;
  percent: string;
};
