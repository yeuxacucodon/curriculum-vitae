import React from "react";
import { Profile } from "@/components";

const ProfileContent: React.FC = () => {
  return (
    <Profile>
      I&apos;m always actively learning new technologies myself. I&apos;m a responsible and passionate person at work. I
      have less than 1 year of experience working with JavaScript, TypeScript, HTML & CSS, React, React Spring, Next.js,
      Linux Terminal, OOP and Git.
    </Profile>
  );
};

export default ProfileContent;
